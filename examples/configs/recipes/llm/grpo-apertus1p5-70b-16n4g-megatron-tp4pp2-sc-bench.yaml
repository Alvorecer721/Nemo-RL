defaults: grpo-apertus1p5-70b-16n4g-megatron-tp2pp4-sc-bench.yaml

# Same benchmark with the replica cut as TP4 x PP2: measured 64 s per steady
# step against 45 s for TP2 x PP4 (jobs 3282045 and 3282077).
policy:
  megatron_cfg:
    tensor_model_parallel_size: 4
    pipeline_model_parallel_size: 2
