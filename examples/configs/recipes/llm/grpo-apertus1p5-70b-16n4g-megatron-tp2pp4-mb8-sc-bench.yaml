defaults: grpo-apertus1p5-70b-16n4g-megatron-tp2pp4-sc-bench.yaml

# Eight sequences per trainer microbatch; the base runs four (35.8 s per
# steady step against 44.8 s at one, job 3282866).
policy:
  train_micro_batch_size: 8
  logprob_batch_size: 8
