defaults: grpo-apertus1p5-70b-bench-sc-smoke-3n4g.yaml

# Same smoke on the 8B step-4200 SFT at TP2/PP2: exercises the PP>1 Apertus refit
# path (xIELU manifest) before the 70B is committed to it.
policy:
  megatron_cfg:
    tensor_model_parallel_size: 2
    pipeline_model_parallel_size: 2
  generation:
    vllm_cfg:
      tensor_parallel_size: 2
      gpu_memory_utilization: 0.6
