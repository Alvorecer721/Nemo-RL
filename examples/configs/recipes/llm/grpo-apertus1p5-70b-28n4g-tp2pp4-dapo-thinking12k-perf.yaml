# Hypothesis: earlier accumulation hides rollout wait at fixed768-response updates.
defaults: grpo-apertus1p5-70b-28n4g-tp2pp4-dapo-thinking12k-control.yaml
grpo:
  max_num_steps: 6
checkpointing:
  enabled: false
policy:
  megatron_cfg:
    env_vars:
      NRL_LOG_LEVEL: DEBUG
async_rl:
  min_groups_for_streaming_train: 24
logger:
  wandb_enabled: false
