name: Request NVSkills CI

on:
  workflow_dispatch:

jobs:
  unavailable-in-fork:
    runs-on: ubuntu-latest
    steps:
      - name: Explain fork behavior
        run: echo "NVSkills CI is NVIDIA-internal and is not configured for this fork."
