# Handoff

Read `session_state.md` first. The active isolated branch is `autoresearch/glm51-r3-10step-20260824`; it stages a fresh ten-step R3-on GLM run from published main. Preserve reservation `SD-69241-apertus-1-5-0`, the GLM conversion cache, and the valid TP2 checkpoint. Do not resume R3 from the saved replay buffer because its trajectories predate route capture. The control is the preserved ten-step R3-off body from job `3147936`: mean KL about `0.00250`, but 7,873 of 3,858,221 valid training tokens had absolute logprob error above `0.5`, 570 exceeded `1.0`, and the maximum reached `37.7`. Require ten complete steps, the established R3 median multiplicative-error gate `<1.02`, direct tail analysis, strict route validation, verified forward replay, no fallback, representative learning signal and a clean terminal artifact. The checkpoint fixes remain published through MCore `9c82d4ca`, Bridge `6b24b9e7` and NeMo-RL PR #26. Use the checked-in container TOML; never submit the raw squashfs path or inherit compute-node Pyxis variables.
