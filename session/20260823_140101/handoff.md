# Handoff

Read `session_state.md` first. The active isolated branch is `autoresearch/glm51-r3-10step-20260824` at `68c9f52c0`. Preserve reservation `SD-69241-apertus-1-5-0`, the GLM conversion cache, and the valid TP2 checkpoint. Generic int16 byte transport, consumer-owned TP padding, process-local trace validation, and compact SingleController logprob-tail metrics are split into small commits. Exact-image job `3182189` passed 76/76 tests with Ruff and formatting, including Gloo and real two-GPU NCCL. Controlled 80-node SingleController/TransferQueue job `3181802` completed its training step with loss `0.07355`, reward `0.14844`, generation KL `0.0003694`, 306,986 valid tokens, and no stale/dropped groups; only the old trace postcheck falsely failed. The next scale rung is 88 nodes: keep 72 training nodes, use 16 inference nodes for two TP32 vLLM replicas, and raise the envelope to 4096 total / 3584 generated tokens. Do not resume R3 from the saved replay buffer because its trajectories predate route capture. Require strict route validation, verified forward replay, no fallback, at least eight learning-signal and nonzero-loss steps, KL below `0.001`, direct compact tail gates, and a clean terminal artifact. The checkpoint fixes remain published through MCore `9c82d4ca`, Bridge `6b24b9e7` and NeMo-RL PR #26. Use the checked-in container TOML; never submit the raw squashfs path or inherit compute-node Pyxis variables.
