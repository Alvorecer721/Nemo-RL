# Handoff

Read `session_state.md` first. The 88-node `windowed` baseline job `3182849` completed ten training steps. The matched `ready_first` job `3184823` completed three correctness-green steps, then the user explicitly cancelled it because the two-TP32-replica rollout fleet remained too slow. Preserve its partial evidence, reservation `SD-69241-apertus-1-5-0`, the GLM conversion cache, and the valid TP2 checkpoint. The next authorized experiment keeps the 72-node trainer, 4096/3584-token envelope, Router Replay, ready-first sampler, and every quality gate fixed, while increasing inference from 16 to 32 nodes (two to four TP32 replicas; 104 nodes total). Exact-image job `3182189` previously passed 76/76 focused tests. The checkpoint fixes remain published through MCore `9c82d4ca`, Bridge `6b24b9e7` and NeMo-RL PR #26. Use the checked-in container TOML; never submit the raw squashfs path or inherit compute-node Pyxis variables. Do not cancel the reservation.
