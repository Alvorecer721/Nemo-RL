# Handoff

Read `session_state.md` first. The frozen 88-node `windowed` baseline is running as job `3182849` from source `6366a7599`; the matched `ready_first` experiment is committed at `d071be968` and queued as job `3184823` with `afterok:3182849`. Preserve reservation `SD-69241-apertus-1-5-0`, the GLM conversion cache, and the valid TP2 checkpoint. The comparison keeps 72 training nodes, 16 inference nodes with two TP32 vLLM replicas, 4096 total / 3584 generated tokens, Router Replay, and every quality gate fixed. Compare exposed generation, step time, evicted and aborted groups, valid-token throughput, learning-signal coverage, KL, and compact logprob tails. `ready_first` should conserve admitted rollout work but cannot pause an individual decode. Exact-image job `3182189` previously passed 76/76 focused tests. The checkpoint fixes remain published through MCore `9c82d4ca`, Bridge `6b24b9e7` and NeMo-RL PR #26. Use the checked-in container TOML; never submit the raw squashfs path or inherit compute-node Pyxis variables.
