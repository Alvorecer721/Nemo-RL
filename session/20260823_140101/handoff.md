# Handoff

Read `session_state.md` first. Preserve reservation `SD-69241-apertus-1-5-0`, the GLM conversion cache, and the valid TP2 checkpoint until NeMo publication is complete. The unpatched TP2 restore failed in `3168898`; clean MCore source SHA `23ae88370` restored all 288 ranks in fresh-allocation job `3169314`, reduced heavy-stage allocation by about 30 GiB, completed training step 2, and exited green. MCore PR #1 is merged at `9c82d4ca`, Bridge PR #5 is merged at `6b24b9e7`, and NeMo pins that Bridge SHA with an unchanged uv lock. The remaining task is publish/CI/merge of the NeMo branch. Use the checked-in container TOML on `srun`; never submit the raw squashfs path or inherit compute-node Pyxis variables.
