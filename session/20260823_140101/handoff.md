# Handoff

Read `session_state.md` first. Preserve reservation `SD-69241-apertus-1-5-0`, the GLM conversion cache, and the valid TP2 checkpoint until publication is complete. The unpatched TP2 restore failed in `3168898`; clean MCore SHA `23ae88370` restored all 288 ranks in fresh-allocation job `3169314`, reduced heavy-stage allocation by about 30 GiB, completed training step 2, and exited green. Publish only that clean SHA to the existing draft MCore PR, then advance Bridge and NeMo-RL gitlinks after merge. The source overlay is validation-only. Use the checked-in container TOML on `srun`; never submit the raw squashfs path or inherit compute-node Pyxis variables.
