# Handoff

Read `session_state.md` first. The active isolated branch is `autoresearch/glm51-r3-10step-20260824`; it stages a fresh ten-step R3-on GLM run from published main. Preserve reservation `SD-69241-apertus-1-5-0`, the GLM conversion cache, and the valid TP2 checkpoint. SingleController/TransferQueue one-step job `3175340` passed setup, refit and rollout but failed before training because its leader-broadcast sent the R3 `routed_experts` tensor as unsupported `torch.int16`. The generic exact-byte wire fix is now green under both Gloo and two-GPU NCCL in exact-image job `3178422`; repeat the 80-node one-step gate before launching ten. Do not resume R3 from the saved replay buffer because its trajectories predate route capture. Require ten complete steps, the established R3 median multiplicative-error gate `<1.02`, direct tail analysis, strict route validation, verified forward replay, no fallback, representative learning signal and a clean terminal artifact. The checkpoint fixes remain published through MCore `9c82d4ca`, Bridge `6b24b9e7` and NeMo-RL PR #26. Use the checked-in container TOML; never submit the raw squashfs path or inherit compute-node Pyxis variables.
