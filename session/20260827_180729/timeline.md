# Timeline

- Audited all 30 branches on `Alvorecer721/Nemo-RL` against `main` and the pull-request ledger.
- Deleted exactly ten merged branch heads with `ahead_by=0`; verified 20 branches remain.
- Confirmed completed job 3186645 used SingleController, TransferQueue, ready-first sampling, 72 training nodes, and 32 inference nodes.
- Confirmed the historical 104-node launch depended on an untracked temporary runner; replaced it with tracked experiment files.
- Confirmed the new experiment changes only inference capacity relative to that 104-node topology: 32 to 64 rollout nodes. The user selected the full ten-step gate rather than the initially staged three-step throughput probe.
- Shell syntax and Python byte-compilation passed. Host pytest was blocked by missing Ray; the exact EDF container performs config preflight before model setup.
- Submitted job `3203601`; Ray connected all `544/544` worker units in 107 seconds, then the driver blocked before model loading in a broad `git status` preflight touching a stalled Capstor inode.
- Cancelled job `3203601` intentionally rather than leave 136 nodes idle; Slurm recorded `CANCELLED by 30214` and no training step ran.
- Replaced the broad driver-side cleanliness query with an explicit runtime-input path set. Recovered the submit wrapper's writable scheduler-log path and non-recursive first-level submodule check.
- Sampled infrastructure health 12 times on 2026-08-28: every Capstor, Git, Iopsstor, and Slurm probe succeeded with stable low latency.
- Submitted two sequential exact-image 136-node Pyxis fan-out canaries on 2026-08-29. Jobs `3215160` and `3215161` both passed (`0` from `srun`), producing 136/136 completion markers from 136 unique nodes in 52 and 44 seconds respectively; neither produced a nonempty task error. This clears the earlier `520/544` worker startup failure as transient infrastructure evidence rather than a deterministic image or capacity failure.
- User authorized an overnight matched upstream-feature campaign: preserve the current 136-node baseline, sync NeMo-RL plus certified Bridge/Megatron pins in isolation, rebuild the exact image, measure a matched post-sync baseline, then enable telemetry/fleet health and deliberately remove one vLLM shard. Recovery requires a post-failure completed training step; throughput claims require matched topology and token envelope. The morning handoff must teach the mechanisms and open questions, not only report outcomes.
- Restored the GLM scale contract from three to ten steps with an acceptance floor of eight learning-signal, nonzero-loss, and nonzero-gradient steps.
- Submitted fixed-contract job `3217346`. Ray brought up all 544 workers and vLLM completed JIT warmup, but the first SingleController logprob fetch failed before step 1: `_broadcast_batched_data_dict` passed Router Replay `torch.int16` directly to NCCL, which rejects `Short` tensors.
- Confirmed the omission by comparing worktrees: signed commits `ce23215a4`, `070abefa6`, and `314e22e20` already implemented byte transport, shared contiguous byte helpers, an actual two-rank NCCL regression, and topology-aligned TQ padding, but the scale branch did not contain them. Replayed those commits and made every experiment-branch commit DCO-compliant.
- One-node probe `3217571` failed before `srun` because the submitting container leaked Slurm's internal Pyxis variables and duplicated `--environment`. Resubmitted with those variables removed; exact-EDF job `3217574` printed `int16_nccl_round_trip=OK` and completed the two-rank NCCL barrier.
- Job `3217663` completed all ten matched 72+64 SingleController/TransferQueue steps. The corrected trace validator passes 288,400 Router Replay records; generation KL stayed `0.000366-0.000448`, and every step had nonzero loss, gradient, signed advantages, and positive reward.
- Inspected Swiss AI's public `model-launch` GLM-5.1 recipes. BF16 serving uses eight GH200 nodes with TP32; FP8 uses four nodes with TP16. Both enable native EAGLE/MTP-style speculative decoding.
- Eliminated the proposed TP/PP topology sweep analytically. BF16 weights are 1.508 TB (47.1 GB/GPU at TP32, 94.2 GB/GPU at TP16 before runtime state), while NeMo-RL's NCCL reshard fail-closes on generation PP greater than one. EP32 is retained because it leaves eight whole experts per rank instead of TP-sharding all 256 experts.
- Started the single retained challenger on `autoresearch/2026-08-29-glm51-mtp3`: three native MTP draft tokens plus TP32 local argmax reduction, with acceptance-counter, checkpoint-MTP, Router Replay, KL, learning-signal, and terminal gates. Slurm was temporarily unresponsive during preparation; no job was submitted into the unobservable interval.
