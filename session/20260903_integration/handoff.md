# Handoff

## Resume From Here
Branch `integration/glm51-on-upstream-a952` (worktree `nemo-rl-worktrees/glm51-on-upstream-a952`) is the single line of record: upstream main `b7ce030b2` + the hermetic CSCS image `3868458efa37-cbbee0e35734` + the GLM-5.1 SingleController campaign, deep-reviewed and simplified. Certified HEAD `33c1e8f5f` (probe 3278193). Campaign worktree retired; its ledger lives in `session/20260831_023449/`. Submodules are initialised at the exact pins; the 136-node launcher's preflight passes in dry run.

## Next Actions
- 136-node MTP3+fused rerun from this worktree (`infra/slurm/cscs/autoresearch/submit_glm51_sc_mtp3.sh`) as the certified baseline on the new image, then the stream8 pair.
- Consolidate the launch contract into named profiles (wrappers/runner/tests carry ~10 edit sites per knob); record `NRL_REFIT_NUM_STREAMS` in the profile and terminal payload.
- Promote a PP2 Apertus refit probe to a permanent rung; move the Apertus scratch checkout onto this branch and run its fixgate probe.
- Reconcile `session/20260827_180729/experiments.tsv` with this line. Refit budget inversion (controller 360 s < 2x330 s settle) still open.

## Watch Outs
- Freeze the committed tree while a probe runs (tests import the live worktree). Untracked session notes are safe.
- Merge this branch with a merge commit, never squash: two upstream merge commits anchor future syncs.
- Never cancel reservation SD-69241-apertus-1-5-0; keep the GLM checkpoint and conversion cache.
- The `v0.7.0` checkout holds the main `.git` for every worktree; clean its tree, never delete it.

## Status 2026-09-03 10:00
- Integration branch HEAD `080777856`: 6 campaign cherry-picks + 5 merge-loss restorations (SC advantage clipping, collector diagnostics + pause race, pad-seqlen idempotency, fail-closed SC guards, restored tests). Lint/compile/diff-check green on host.
- Certification probe job `3275013` (3274996 cancelled: HEAD moved) (new image via docker/nemo_rl_vllm0251.toml, fingerprint gate, focused unit tests, post-hoc revalidation of 3240688) launched from `.tmp/glm51_integration_probe.sbatch`; logs under `.tmp/slurm-logs/glm51-integ-probe/`.
- Submodules intentionally left uninitialized (fingerprint reads pinned gitlinks; GitHub clone was rate-limited). Do not `--depth 1` init them: it checks out wrong tips.
- Probe 3275013 FAILED on my own guard: upstream's SC GRPOConfig defaults `async_grpo` to a disabled AsyncGRPOConfig, so "reject non-null" broke every SC fixture. Fixed in `176db0e49` (reject only `async_grpo.enabled=true`; tests cover enabled=rejected / disabled=inert). Probe also had a global `-k` that deselected 903 tests; rewritten as separate pytest invocations. Resubmitted as job 3275052 pinned to `176db0e49`.

## Status 2026-09-03 ~10:50
- Probe 3275847 (HEAD 0b73441bd): everything green except two collector tests that contradict each other (upstream hand-back vs fork raise on worker-start failure); kept upstream's contract, retired the stale fork test (46f1e2ac2).
- Deep code review final: 10 findings; addressed non-finite tail deltas (count metric + validator gate, 8a693ea8c), fused resolver + single unsupported-settings raise (6fbd382ae), comment/stub cleanup (3b21105ec), explicit GLM preflight envelope + terminal.json fields + stream8 wrappers (ff44c2e2c). Left as-is: `_clip_grpo_advantages` shared helper (call sites, not copies; upstream-owned loops).
- Probe 3275921 queued on HEAD ff44c2e2c (26 commits over d9a1ab7d4). Tree frozen while it runs.
- For the eventual 136-node launch from this worktree: submit_glm51_sc_scale.sh refuses uninitialized submodules -> init them with `--reference` to the sync worktree's checkouts (GitHub anonymous clone was rate-limited; never `--depth 1`).

## Certified 2026-09-03 11:36
- Probe 3276048 on HEAD `7b3309794` (27 commits over d9a1ab7d4): fingerprint OK, unit blocks 31/3/117/9/7/31/747 passed, post-hoc revalidation of 3240688 OK, glm51_integration_probe=OK. Image: nemo-rl-apertus-vllm-0.25.1-3868458efa37-cbbee0e35734.
- Next: simplify pass (may add commits -> re-probe), then the 136-node MTP3+fused rerun as the new certified baseline (init submodules with --reference first), then retire the campaign worktree and the stale v0.7.0 checkout.
- 2026-09-03 11:51: simplify pass (four reviewers: reuse, simplification, efficiency, altitude) landed as fed2b7779 (tails reducer: np.quantile, one dict, all-finite fast path; bit-identical on fixtures), 55063cba2 (SC guard hunk is now a pure insertion after upstream's comprehension; stop_at_validation_* and legacy async_grpo join the GRPO-only block; fused resolver in grpo.py's shape), dd6636515 (collector: _paused_for_limits dropped in favour of upstream's is_set() read, upstream except block restored), 0a032f300 (R3_MAX_SEQ_MULT_CEILING named; runner parses the fused flag with a [[ ]] guard; scale test wrapper defaults + dataclass-equality pair assertion). Net -63 lines. Skipped on purpose: named-profile refactor of the launch contract (wrappers/runner/tests carry ~10 edit sites per knob; shell path has no probe coverage, so it is the next phase, not this diff); making count_nonfinite required in the validator (the probe revalidates the 3240688 dump, which predates that metric); asyncio.to_thread around reduce_advantage_pump_metrics (upstream line, ~50 ms/step); logging->print in the reshard synchronizer (caplog test); deleting test_disabled_legacy_async_block_is_inert (regression guard for probe 3275013); recipe rename/intermediate fused recipe (ledger + submit paths reference the current names). Open observations: NRL_REFIT_NUM_STREAMS=8 is exported by the stream8 wrappers but recorded in neither the profile nor terminal.json. Re-certification probe 3277343 on HEAD 0a032f3006710b41ae8f11690ddb1586f0f2f804; tree frozen until it reports.
- 2026-09-03 12:xx: re-certification probe 3277343 green on HEAD `0a032f300` (32 commits over d9a1ab7d4): fingerprint OK, unit blocks 31/3/117/9/7/31/747 passed, post-hoc revalidation of 3240688 OK (max_seq_mult 1.0453, token_mult median 1.0112), glm51_integration_probe=OK, 9m10s wall. Tree unfrozen. This is the certified baseline for the 136-node MTP3+fused rerun.
- 2026-09-03 12:xx: retirement done. Campaign ledger closed on its branch (9e0cbca70) and carried here (95e94266c; PNGs untracked in session/20260831_023449/, campaign probe logs archived under .tmp/archive-glm51-mtp3-fused/); worktree glm51-mtp3-fused removed, branch autoresearch/2026-08-31-glm51-stream8-pair retained; four dead /tmp worktree entries pruned. Submodules initialised offline from the sync worktree's module stores at the exact pins (Bridge 3ce0cddef with nested Megatron-LM 7851ef7cb, Automodel 1814c6c93, Gym c3bac9631, kernels 90b92d6b7; needed -c protocol.file.allow=always); URLs re-synced to .gitmodules. Note: the v0.7.0 checkout holds the main .git for every worktree, so "retiring" it can only mean cleaning its working tree, never deleting the directory.
- 2026-09-03: upstream main moved to b7ce030b2 (3 commits past a95290529, fetched into upstream/main). b7ce030 "fix(sc): apply advantage clipping and overlong filtering (#3927)" is upstream's version of our a11e68f5f (+ stub commit 03cbca432): same `_clip_grpo_advantages` call, placed after the OPD statistics so teacher stats see unclipped estimator output, and `masked_advantages` logged from the clipped values. Numerically inert for the GLM recipes (advantage_clip_low/high null, overlong_filtering false). At the next sync drop a11e68f5f and 03cbca432 in favour of upstream's hunk; expect a conflict in the advantage stage otherwise. The other two commits (multimodal Megatron inference #3920, CI fix #3967) do not touch our path.
- 2026-09-03 12:57: merged upstream/main b7ce030b2 (3 commits: #3927 SC advantage clipping, #3920 multimodal Megatron inference, #3967 CI) as 33c1e8f5f23723c0270b1621328a82001400fb1d. Conflicts resolved: single_controller.py takes upstream's clipping placement (fork's early clip removed), venvs.py keeps the fork's uv pin + fingerprint protocol and adds upstream's add_hf_modules_cache_to_pythonpath + extra_env_vars, actor tests take upstream's GRPOConfig stubs and drop the fork's duplicate clipping test, test_venvs.py = fork file + upstream's three new tests. Fingerprint inputs unchanged (no pyproject/uv.lock/submodule change), image kept. Re-certification probe 3278193; tree frozen until it reports.
- 2026-09-03: post-merge probe 3278193 green on `33c1e8f5f`: fingerprint OK, blocks 33/3/117/9/7/31/750 passed, 3240688 revalidation OK, glm51_integration_probe=OK, 9m16s. Certified HEAD for the 136-node rerun is now 33c1e8f5f (upstream main b7ce030b2 merged, image unchanged).
- 2026-09-03: session record committed (a7837fae5), branch pushed, draft PR opened: https://github.com/Alvorecer721/Nemo-RL/pull/28 (base main, fast-forward, MERGE COMMIT ONLY). Stays draft until the 136-node rerun lands.
- 2026-09-03 ~13:50: PR #28 CI triage. Title >80 chars (semantic check counts length) -> shortened via REST (`gh pr edit` fails on a GraphQL projects-classic error; use `gh api -X PATCH repos/.../pulls/28`). Lockfile, submodule-fast-forward and secrets checks all died in the recursive checkout: the Bridge pin 3ce0cddef was a dangling object on Alvorecer721/Megatron-Bridge (no branch reached it; runner got "not our ref"). Published the pins as branches `integration/glm51-on-upstream-a952` on Alvorecer721/Megatron-Bridge (3ce0cddef, pushed from the non-shallow main-repo store v0.7.0/.git/modules/3rdparty/Megatron-Bridge; the worktree's shallow store cannot push it) and on Alvorecer721/Megatron-LM (7851ef7cb). `gh run rerun` also fails silently; use `gh api -X POST .../actions/runs/<id>/rerun-failed-jobs`.
- 2026-09-03 ~13:55: secrets detector was failing only because the baseline carried a stale test_version_check.py entry after the sync. Regenerated with detect-secrets 1.5.0 in an isolated venv, keeping the baseline's own `\.oms\.sig$` exclusion (the template's printed command omits it and would list every skill signature blob); no real secrets. Remaining PR checks all green.
